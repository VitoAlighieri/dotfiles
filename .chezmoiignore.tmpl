#============ GLOBAL ============#

# Documentacion y metadatos del repo: no se despliegan al $HOME.
README.md
LICENSE
.vscode

# Nota: .chezmoidata/ y .chezmoiscripts/ los excluye chezmoi
# automaticamente (no hace falta listarlos aqui).

#============ LINUX ============#
{{ if eq .chezmoi.os "linux" }}
AppData
.config/powershell
{{ end }}
